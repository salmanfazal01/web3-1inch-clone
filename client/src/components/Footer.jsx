import { Box, Divider } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Box sx={{ height: 500 }}>
      <Divider />
    </Box>
  );
};

export default Footer;
